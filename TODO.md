Протестируй https://stellarburgers.education-services.ru/ API для Stellar Burgers.
Пригодится документация API https://code.s3.yandex.net/qa-automation-engineer/python-full/diploma/Api-Stellar_Burgers.pdf. В ней описаны все эндпоинты сервиса. Тестировать нужно только те, которые указаны в задании. Всё остальное — просто для контекста.
Создание пользователя:
создать уникального пользователя;
создать пользователя, который уже зарегистрирован;
создать пользователя и не заполнить одно из обязательных полей.
Логин пользователя:
вход под существующим пользователем;
вход с неверным логином и паролем.
Создание заказа:
с авторизацией;
без авторизации;
с ингредиентами;
без ингредиентов;
с неверным хешем ингредиентов.
Что нужно сделать
Создай отдельный репозиторий для тестов API.
Подключи библиотеки: pytest, requests и allure-pytest.
Напиши тесты.
Сделай отчёт в Allure.

Чек лист для задания
Автотесты API
Названия пакетов, файлов, классов, методов, тестов и переменных —
корректные.
Для каждого эндпоинта тесты лежат в отдельном классе.
Тесты запускаются и проходят.
Написаны все тесты, указанные в задании.
Критерии оценки 1
В тестах проверяется тело и код ответа.
Все тесты независимы.
Нужные тестовые данные создаются перед тестом и удаляются после
того, как он выполнится.
Сделан Allure-отчёт. Отчёт добавлен в пул-реквест.
Тесты лежат в tests .
В проекте нет ничего лишнего. Например, неиспользуемых зависимостей
в requirements.txt , неиспользуемых импортов, закомментированных блоков
кода, файлов настроек IDE. Можешь оставлять комментарии, которые
объясняют твоё решения. Это не будет ошибкой.

В качестве примера реализации задачи можно использовать /Users/sergeymac/dev/Python/Sprint_7